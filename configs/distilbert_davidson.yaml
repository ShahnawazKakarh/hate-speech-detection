run_name: distilbert_davidson
model_type: distilbert
dataset: davidson
seed: 42
wandb: false
wandb_project: hate-speech-detection
wandb_entity: qapulsebysk
model:
  model_name: distilbert-base-uncased
  max_length: 128
  learning_rate: 2.0e-5
  batch_size: 32
  eval_batch_size: 64
  epochs: 3
  weight_decay: 0.01
  warmup_ratio: 0.1
  seed: 42
  fp16: false   # set true on CUDA
  report_to: none  # "wandb" to log
